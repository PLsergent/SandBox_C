#include <stdio.h>

int main() {
    int number, base, output, output_base; 
    printf("Enter a number : ");
    scanf("%d", &number);

    printf("Enter the input base (2, 8, 10, 16) : ");
    scanf("%d", &base);
    printf("Enter the output base (2, 8, 10, 16) : ");
    scanf("%d", &output_base);


    return 0;
}